# Core dependencies (install first)
pandas
numpy
matplotlib
networkx
scipy
python-louvain
pyvis
torch
scikit-learn
xgboost
seaborn
umap-learn
tqdm

# Graph dependencies (install after torch)
torch-geometric

# Temporal graph dependencies (install manually after other packages)
# torch-geometric-temporal requires torch-sparse to be built, which fails in isolated pip builds
# Install manually with: pip install torch-geometric-temporal --no-build-isolation
# torch-geometric-temporal